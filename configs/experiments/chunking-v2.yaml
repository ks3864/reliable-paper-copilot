experiment:
  name: regression-eval
  pipeline_version: chunking-v2
  description: Regression candidate using overlap-aware reliability chunking.

retrieval:
  embedding_model: all-MiniLM-L6-v2
  chunk_profile: chunking-v2

answering:
  generator: simple
  judge: mock

evaluation:
  top_k: 5
  use_answer_quality_judge: true
